# Syntaxia

Website for **Syntaxia**.

## Stack

- Next.js (App Router)
- Vercel hosting
- Supabase (auth / database)

## Local

```bash
npm install
cp .env.example .env.local
npm run dev
```

## Deploy

Pushes to `main` deploy via Vercel (GitHub connected).
